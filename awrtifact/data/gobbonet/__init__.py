# The provision-repo seed assets (mod + gate page). Declared so setuptools
# ships them on every build env — an undeclared data dir silently drops from
# the wheel on newer setuptools (measured 2026-08-27 on the mirror build).
